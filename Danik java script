let cartCount = 0;
let totalPrice = 0;

function addToCart(price) {
    cartCount++;
    totalPrice += price;
    document.getElementById('cart-count').innerText = cartCount;
    alert(`Added to cart! Total items: ${cartCount}, Total price: $${totalPrice}`);
}
